/*
 * DatasetPage Messages
 *
 * This contains all the text for the DatasetPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.DatasetPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the DatasetPage container!',
  },
});
