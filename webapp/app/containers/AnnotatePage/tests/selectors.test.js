// import { selectAnnotatePageDomain } from '../selectors';

describe('selectAnnotatePageDomain', () => {
    it('Expect to have unit tests specified', () => {
        expect(true).toEqual(false);
    });
});
