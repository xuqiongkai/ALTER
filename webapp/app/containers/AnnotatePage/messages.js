/*
 * AnnotatePage Messages
 *
 * This contains all the text for the AnnotatePage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.AnnotatePage';

export default defineMessages({
    header: {
        id: `${scope}.header`,
        defaultMessage: 'This is the AnnotatePage container!',
    },
});
